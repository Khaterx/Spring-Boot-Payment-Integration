spring.application.name=demo
server.port=8050
# PayPal Configuration
paypal.client-id=
paypal.client-secret=
paypal.base-url=https://api-m.sandbox.paypal.com

# Stripe Configuration
# Replace with your Stripe credentials
stripe.secret.key=sk_test_5
stripe.publishable.key=pk_test_51TKx00RsP4kJBEgLw0
stripe.webhook.secret=whsec_

# Stripe Checkout Redirect URLs
stripe.success.url=http://localhost:8050/api/payments/stripe/success
stripe.cancel.url=http://localhost:8050/api/payments/stripe/cancel

# Default: H2 in-memory (for local dev without Docker)
# Use docker profile for PostgreSQL: SPRING_PROFILES_ACTIVE=docker
spring.datasource.url=jdbc:h2:mem:paymentdb;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
